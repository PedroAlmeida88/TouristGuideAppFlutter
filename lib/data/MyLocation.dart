class MyLocation {
  final String name;
  final String description;
  final String photoUrl;
  final double latitude;
  final double longitude;

  MyLocation(this.name, this.description, this.photoUrl, this.latitude, this.longitude);
}
